import React from "react";
import Layout from "../../layout/layout";

type Props = {
  addTool: any
}

export function Tool({ addTool }: Props) {
  return (
    <main className="flex">
      <Layout>
        ss
      </Layout>
    </main>
  );
}

export default Tool;
